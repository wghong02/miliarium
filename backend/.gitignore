# macOS
.DS_Store

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo

# Logs (Firebase CLI writes these into the repo root)
firebase-debug.log
firebase-debug.*.log
firestore-debug.log
ui-debug.log
*.log

# Local Firebase artifacts
.firebase/
.runtimeconfig.json

# The Firebase CLI materializes these at the config root during
# `emulators:exec` (functions emulator); the real npm package lives in
# functions/, so these are throwaway.
/node_modules/
/package.json
/package-lock.json

# Environment
.env
.env.local
.env.*.local

# Generated by some Firebase CLI versions
*.cache
