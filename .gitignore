# Repo-level ignores. Per-project rules live in ios/.gitignore and
# backend/.gitignore.

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Xcode derived data (if it ever lands at the repo root)
.derivedData/
DerivedData/

# Claude Code local settings
.claude/settings.local.json
